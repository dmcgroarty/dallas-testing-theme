<h3>{{ section.settings.heading }}</h3>

{% for block in section.blocks %}
  <img src="{{ block.settings.image.image_example | image_url: 'master' }}" 
    />
{% endfor %}

{% schema %}
{
  "name": "testing",
  "settings": [
    {
      "type": "text",
      "id": "heading",
      "label": "Heading for section"
    }
  ],
  "blocks": [
    {
      "type": "image",
      "name": "image block",
      "settings": [
        {
          "type": "image_picker",
          "label": "Your image",
          "id": "image_example"
        }
      ]
    }
  ]
}
{% endschema %}